# MOVED → `/BACKLOG.md`

Backend cleanup status has been merged into the single source of truth at the repo root:
**[`/BACKLOG.md`](../BACKLOG.md)**.

- Clusters A, B#1, B#2 → §Archive in `/BACKLOG.md`.
- The one open backend item (run **prod** `migrate-tag-to-priority.js`) → **P0** in `/BACKLOG.md`.

Bug-detail rationale still lives in `server/backend.md` §"Bug Fix Plan". Update `/BACKLOG.md`, not this file.
